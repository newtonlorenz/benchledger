{
  "name": "@benchledger/server",
  "version": "0.1.0",
  "description": "Fastify API and application host for BenchLedger",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/main.ts --demo",
    "start": "node dist/main.js",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/swagger": "^9.5.1",
    "@benchledger/api-contract": "0.1.0",
    "@benchledger/application": "0.1.0",
    "@benchledger/mcp": "0.1.0",
    "@benchledger/runtime": "0.1.0",
    "fastify": "^5.6.1"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}

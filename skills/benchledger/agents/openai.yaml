interface:
  display_name: "BenchLedger"
  short_description: "Evidence-backed maker project decisions"
  brand_color: "#3F5F55"
  default_prompt: "Use $benchledger to answer the current maker-project request from the existing inventory and revision. Perform only the relevant stage, preserve evidence and approval boundaries, and report the verified result."

dependencies:
  tools:
    - type: "mcp"
      value: "benchledger"
      description: "BenchLedger MCP server for inventory, exact products, projects, BOMs, build configurations, artifacts, supplier observations, and reconciliation."
